export interface MaterialInterface {
    height: number
    id: number
    image_url: string | ArrayBuffer
    is_identical: boolean
    name: string
    rotation: number
    walkable: boolean
    width: number
}